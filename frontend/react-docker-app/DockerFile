FROM node:20-slim

WORKDIR /app

# Only copy package files and install cleanly
COPY package*.json ./
RUN npm install --legacy-peer-deps --omit=optional

# Now copy the rest of the code (not node_modules)
COPY . .

CMD ["npm", "run", "dev"]